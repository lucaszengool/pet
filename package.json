{
  "name": "pet-platform",
  "version": "1.0.0",
  "description": "宠我吧 - 一站式宠物服务平台",
  "private": true,
  "scripts": {
    "postinstall": "cd pet-app && npm install && cd ../pet-app-server && npm install",
    "build:frontend": "cd pet-app && npm run build:h5",
    "build": "npm run build:frontend && mkdir -p pet-app-server/public && cp -r pet-app/dist/build/h5/* pet-app-server/public/",
    "start": "cd pet-app-server && npm start",
    "dev": "cd pet-app-server && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
